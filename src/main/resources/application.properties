# spring.application.name=backend


# # server.port=50012
# server.port=${PORT:8080}



# # # PostgreSQL connection
# # spring.datasource.url=jdbc:postgresql://localhost:5432/cutsizer
# # spring.datasource.username=postgres
# # spring.datasource.password=vijay@30


# # # PostgreSQL connection

# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.url=jdbc:postgresql://168.231.120.70:5432/winsizer_cutsizer
# spring.datasource.username=postgres
# spring.datasource.password=qGe]Ax+$nbbQe6qf

# # JPA / Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# # Logging
# logging.level.com.cutsizer=DEBUG
# logging.level.org.springframework.security=DEBUG

# # JWT Configuration
# jwt.secret=cutsizer-secret-key-change-this-in-production-must-be-at-least-256-bits-long-for-security
# jwt.expiration=604800000


spring.application.name=backend

# Render server port
server.port=${PORT:8080}

# PostgreSQL on Render
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=${DATABASE_URL}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.com.cutsizer=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration
jwt.secret=cutsizer-secret-key-change-this-in-production-must-be-at-least-256-bits-long-for-security
jwt.expiration=604800000
